services:
  sync-service:
    build: .
    container_name: sync-service
    restart: unless-stopped
    env_file: .env
    environment:
      HEALTH_PORT: "8090"
      READ_TIMEOUT: "300"
    ports:
      - "8090:8090"
